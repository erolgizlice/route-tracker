# Gradle / build output
.gradle/
.kotlin/
build/

# Local machine configuration — holds sdk.dir and MAPS_API_KEY, never committed
local.properties

# Signing material: the release keystore and its passwords stay on the author's machine
*.jks
*.keystore

# IDE
.idea/
*.iml
captures/

# OS
.DS_Store

# Claude Code local permissions (machine-specific paths)
.claude/settings.local.json
